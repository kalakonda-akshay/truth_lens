import { AppShell } from "@/components/AppShell";
export default function SettingsPage() {
  const services = [["Sightengine", "Image and video AI detection"], ["Reality Defender", "Synthetic audio detection"], ["TruthLens URL Engine", "Local phishing analysis"], ["TruthLens Email Engine", "Local scam detection"]];
  return <AppShell title="Platform Settings" subtitle="Production integrations and investigation preferences."><div className="grid gap-5 lg:grid-cols-2"><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-black">API Integrations</h2><div className="mt-5 divide-y divide-slate-100">{services.map(([name, detail]) => <div key={name} className="flex items-center justify-between py-4"><div><p className="font-bold">{name}</p><p className="text-xs text-slate-500">{detail}</p></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-black text-emerald-700">Configured server-side</span></div>)}</div></section><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-black">Investigation Defaults</h2><div className="mt-5 space-y-4">{["Require explainable evidence", "Fail closed when provider analysis fails", "Include recommendations in reports", "Show cyber-awareness warning"].map((setting) => <label key={setting} className="flex items-center justify-between rounded-xl bg-slate-50 p-4 text-sm font-bold"><span>{setting}</span><input type="checkbox" defaultChecked className="h-5 w-5 accent-blue-600" /></label>)}</div></section></div></AppShell>;
}
